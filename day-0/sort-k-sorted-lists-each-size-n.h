#include <vector>

std::vector<int> sort_k_lists(const int& k,
    const vector<int>& input_list) {
  // Assuming that each list is of same size, 
  // otherwise we need n
  int size_each_list = input_list.size()/k;

}
